// import { Currency } from '@/modules/currencies/schemas/currency.schema';
// import { Document } from 'mongoose';

// type CurrencyMock = Omit<Currency, keyof Document>;

// export const createMock = (overrides: Partial<CurrencyMock> = {}) => ({
//   name: 'Test Currency',
//   acronym: 'TC',
//   ...overrides,
// });
